{
  "buildCommand": "bun install",
  "outputDirectory": "public",
  "installCommand": "bun install",
  "runtime": "bun",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
