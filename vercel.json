{
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "framework": "static",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ],
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  }
}
